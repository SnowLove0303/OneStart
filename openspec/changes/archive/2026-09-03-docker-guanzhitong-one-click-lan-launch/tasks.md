## 1. Docker 与 Wi‑Fi 生命周期编排

- [x] 1.1 在 `System\Workflow-Launcher.ps1` 增加一键启动编排：记录目标容器原状态，复用现有 Web 健康检查和 LAN 开启逻辑，仅在完整成功后打开本机页面并输出局域网地址；用静态检查和失败路径审查确认不创建第二容器/端口
- [x] 1.2 增加一键关闭编排：先关闭专用 LAN 防火墙规则，再停止同一目标容器，并在失败时返回非零；用函数级回读确认关闭顺序和错误传播
- [x] 1.3 为一键启动补充失败回滚：若本次启动了原本停止的容器但 LAN 或最终验证失败，则关闭规则并停止该容器；用受控失败场景或代码路径检查确认原本已运行的容器不会被误停

## 2. 统一启动器入口与说明

- [x] 2.1 在命令解析中加入 `start guanzhitong-lan`、`guanzhitong-lan` 和 `stop guanzhitong-lan`，保持 `wll web`、`wll lan on/off/status` 兼容，并为失败返回非零退出码；用命令行冒烟测试验证每个入口映射到唯一编排函数
- [x] 2.2 在交互菜单中加入一键启动和一键关闭选项，保留现有基础 LAN 操作编号；用菜单动作静态映射检查确认无编号冲突
- [x] 2.3 更新 `System\README.md`，写明推荐的一键启动/关闭流程、管理员权限、唯一 Docker 对象、端口和安全边界；用 `rg` 检查命令、端口和路径描述与实现一致

## 3. 静态与真实运行验证

- [x] 3.1 执行 PowerShell AST 解析、`System\assets\syntax-check.ps1`、`git diff --check` 和 `openspec validate --strict`；确认无解析错误、危险 Docker 清理命令或规格不一致
- [x] 3.2 在当前正式根目录和统一版本基线的唯一实例上执行一键启动，回读容器名/镜像、运行与健康状态、8765→18765 映射、本机 HTTP、Private WLAN 子网、专用防火墙规则及 Docker 宽泛 TCP 规则；记录实际 PID/命令来源和结果
- [x] 3.3 执行一键关闭并再次回读，确认专用 LAN 规则关闭、目标容器停止、本机 HTTP 不可用且没有新增容器/端口/规则；若无法用第二台同 Wi‑Fi 设备实测，明确记录该限制而不把主机自测冒充物理 LAN 验证
